name: keg_data_plane

networks:
  keg:
    name: keg-network
    external: true

services:
  keg-data-plane:
    image: kong/kong-event-gateway:1.2.0
    container_name: keg-data-plane
    restart: unless-stopped
    env_file:
      - konnect.env
    environment:
      KEG__OBSERVABILITY__LOG_FLAGS: info,keg=debug
      KEG__RUNTIME__HEALTH_LISTENER_ADDRESS_PORT: "0.0.0.0:8080"
    volumes:
      - ./kongctl/certs:/etc/keg/certs:ro
    ports:
      - "19092-19190:19092-19190"
      - "8180:8080"
    networks:
      - keg

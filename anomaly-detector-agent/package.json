{
  "name": "anomaly-detector-agent",
  "version": "1.0.0",
  "description": "Agent Smith Protocol — monitors sentinel.readings via KEG, judges anomalies with an LLM, and fans out orchestration events to independent responder agents",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "volcano-sdk": "^1.1.0",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "@types/node": "^22.15.0"
  }
}
